# blessed-pipeline
a modular, scalable GitHub Actions pipeline composed of reusable workflows (best for platform engineering) and top-level orchestrator workflows that call them. It implements CI, security scanning, IaC validation &amp; plan, and gated production deploys with observability hooks and artifacts
